# Local development environment — safe defaults.
# Real secrets belong in .env.local (gitignored). See .env.example for reference.

VITE_SITE_URL=http://localhost:4028

# Google Analytics 4 Measurement ID (public, not a secret).
# GA4 loads only after cookie consent; leave blank to disable analytics.
VITE_GA4_MEASUREMENT_ID=G-9TDJLZYRKL
